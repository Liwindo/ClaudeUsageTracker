# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
*.egg

# Tests
.pytest_cache/
.coverage
htmlcov/

# Config (contains no secrets, but personal — user may want to exclude)
# Uncomment the next line to ignore your local config:
# AppData/

# PyInstaller (spec is kept in the repo, build artefacts are not)
*.spec.bak

# Static analysis output
static_analysis_semgrep*/

# Local AI assistant instructions (not part of the published repo)
CLAUDE.md

# IDE
.vscode/
.idea/
*.swp

# C# variant (csharp/)
bin/
obj/
publish/
*.user
.vs/

# Update-signing PRIVATE keys — MUST never be committed. The signing key is
# offline-only; only its public half lives in the repo (UpdateKeys.json).
*.pem
*.key
update-signing-key*
